The method-aligned bulk-fit DAG is defined in `workflow/Snakefile`.

The previous modular `.smk` rules targeted historical empirical/lite/full
outputs and were removed to avoid maintaining two incompatible workflow
contracts.
